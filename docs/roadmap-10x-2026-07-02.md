# 10x Roadmap — 2026-07-02

A feature roadmap derived from a full codebase review (backend, frontend,
eval harness, docs). Companion to [roadmap.md](roadmap.md) — that file records
what's been tried and closed; this one is the forward plan for making the
product an order of magnitude more valuable.

## The thesis: three ceilings

Today the product is a **stateless, anonymous, one-shot novelty tool**: type a
Last.fm username → obscurity index + up to 25 obscure artist recs → share a
PNG → leave. Nothing changes on the next visit. The review found three
structural ceilings, and every 10x feature attacks one of them:

1. **Data ceiling.** Candidates come exclusively from Last.fm `artist.getsimilar`,
   which is popularity-biased and genre-clustering. Proven at n=348: reach is
   flat (~0.05) across every scoring lever; pure-discovery hits are pinned at
   ~78 no matter what. No ranking change fixes this — only a richer graph (CEGE).
2. **Signal ceiling.** The engine emits recommendations and never learns
   anything back. No clicks, saves, dismissals, or follows are captured. The
   eval's ground truth is *unattributed* holdout replays — a hit counts whether
   the engine caused it or the user found the artist elsewhere
   (`eval/metrics.py:5-8` admits the confound). We cannot measure, let alone
   optimize, the thing the mission cares about: **did we cause a discovery?**
3. **Audience ceiling.** Requires an existing Last.fm account with scrobble
   history — a small niche. No accounts, no server-side user state (identity is
   a query param; persistence is localStorage), so there is also no retention
   loop for the users we do get.

Corollary from the mission ("help under-discovered artists reach their true
1000 fans"): the product currently serves only the *listener* half. The artist
half — the actual beneficiary — has no surface at all.

**Dormant assets the review found** (cheap wins, already built):
- Full track pipeline live at `/api/discovery/tracks` (ungated server-side,
  hidden client-side behind `TracksComingSoon`).
- Complete Spotify PKCE OAuth + playlist-creation flow in `frontend/src/lib/spotify.ts`
  — dead code today because only tracks mode reaches it.
- `velocity: Option<f64>` field stubbed on every response item (`models.rs:48`,
  always `None`) — Last.fm exposes no listener time-series, but *we fetch
  `artist.getinfo` listener counts for ~600 artists per request and throw the
  numbers away*. Persisting them builds the missing momentum dataset ourselves.
- Persistent share links + Redis-ready cache landing tonight (see
  [dev-tasks-overnight-2026-07-02.md](dev-tasks-overnight-2026-07-02.md)).

---

## Phase 1 — Close the loop (signal ceiling → data flywheel)

The highest-leverage phase. It converts the app from a demo into a system that
generates the exact data (attributed adoptions) that the eval harness proved it
lacks, and creates the first retention loop.

### F1. Server-side persistence layer (Postgres)

**What:** Add the first database. Postgres (Railway addon), accessed from the
Rust backend via `sqlx`. Tables: `users` (id, lastfm_username, created_at,
settings), `runs` (user, period, appetite, depth_score, top_genres, timestamp),
`recommendations` (run_id, artist, rank, conviction, listeners, scores),
`events` (rec_id, type, timestamp), `artist_observations` (artist,
listeners, monthly_listeners, observed_at) — see F4.

**Why:** every other Phase-1/2 feature needs it; today literally nothing
survives a backend restart except contributed API keys.

**Acceptance criteria:**
- `DATABASE_URL` unset → backend boots and serves `/api/discovery` exactly as
  today (graceful fallback, verified by integration test with the var absent).
- With `DATABASE_URL`: every discovery run inserts one `runs` row + N
  `recommendations` rows; verified by a test that runs a discovery and asserts
  row counts and score fields match the HTTP response.
- Migrations are versioned (`sqlx migrate`) and run automatically on boot;
  restarting the service loses zero rows.
- p50 latency overhead of persistence on `/api/discovery` < 50ms (measured
  before/after on a warm cache hit).

### F2. First-party feedback capture

**What:** Instrument every recommendation surface: listen-link clicks
(Spotify/Last.fm/Bandcamp/This-Is), an explicit **save** (bookmark this
artist) and **dismiss** (not for me) on each `ArtistCard`, and share events.
New endpoint `POST /api/events` (rec_id, event_type); frontend fires it
best-effort (never blocks UI). Saved artists get a "Saved" view.

**Why:** this is the attributed-adoption signal the eval fundamentally lacks.
Dismissals are equally valuable (explicit negatives don't exist anywhere in the
current data).

**Acceptance criteria:**
- Clicking any listen link or save/dismiss writes exactly one `events` row with
  the correct rec_id and type (integration test with a stubbed run).
- Events endpoint rejects malformed/unknown rec_ids (400) and is rate-limited
  per IP; verified by test.
- UI: save/dismiss state survives page reload (server-backed, not localStorage);
  dismissed artists never reappear in future runs for that user (scoring
  exclusion list, verified by two consecutive runs in a test).
- Zero UI regression when backend has no DB: buttons hide or no-op gracefully.

### F3. Closed-loop adoption tracking ("Your discoveries")

**What:** A scheduled job (tokio cron in-backend) re-polls each active user's
recent scrobbles weekly and marks a recommendation **adopted** when the user
scrobbled that artist ≥3 times *after* the rec was shown (mirrors the eval's
`min_future_plays=3` definition, but now attributed and per-rec). Surface it:
a "Your discoveries" page — "You've adopted 7 artists the engine showed you,
mean listeners 9.4K" — plus obscurity-index-over-time from `runs`.

**Why:** this is the product's proof-of-value made visible to the user (the
retention hook), and it produces a **first-party attributed eval metric** —
adopted-per-shown — that the n=348 harness cannot compute. Because users are
scrobblers, attribution comes free: rec shown at T, scrobbles after T are
observable.

**Acceptance criteria:**
- Job correctness: unit test with fixture scrobble data — artist recommended at
  T and scrobbled 3+ times in (T, T+7d] → marked adopted; scrobbled before T or
  <3 times → not.
- A user with ≥2 runs sees an index-over-time sparkline and a discoveries list;
  verified in headless-browser QA.
- New eval capability: `eval/` gains a `first_party_report.py` that reads the
  events/adoptions tables and prints adopted-per-shown, save-rate, dismiss-rate
  with bootstrap CIs — the online counterpart of obscW@k. Runs against staging
  data without touching the existing harness.
- Privacy: per-user data export + delete endpoint (`GET/DELETE /api/me/data`),
  tested.

### F4. Listener-count time series → real velocity signal

**What:** Persist every `artist.getinfo` observation the pipeline already makes
(artist, listeners, monthly listeners, timestamp) into `artist_observations`
(dedup: one row per artist per day). After ~60 days of accumulation, compute
listener growth rate and populate the stubbed `velocity` field; show a
"rising" badge on artists with high relative growth.

**Why:** Last.fm has no listener time-series API — this dataset cannot be
bought, only accumulated, and the backend already fetches ~600 getinfo calls
per discovery run and discards them. It's also the seed of the artist-side
product (F9): "your listener growth curve."

**Acceptance criteria:**
- Observations written as a side effect of normal discovery runs with zero
  added Last.fm calls (verified: request count unchanged in a counting test).
- Dedup: same artist observed twice in one day → one row (test).
- After backfill window, `velocity` is non-null for artists with ≥2
  observations ≥14 days apart; formula unit-tested on fixtures.
- The badge appears only above a growth threshold; QA screenshot check.

## Phase 2 — Widen the front door (audience ceiling)

### F5. Spotify listening import (no Last.fm required)

**What:** "Continue with Spotify" on the landing page: user OAuth (the PKCE
plumbing in `lib/spotify.ts` already does the hard part), pull top artists
(short/medium/long term) + recently played, map them to the seed format
(`log2`-weighted), and run the same pipeline. Backend gets a
`POST /api/discovery/spotify` accepting the seed payload (token stays
client-side; backend never sees it).

**Why:** the single biggest TAM unlock — Spotify's user base vs Last.fm's is
orders of magnitude. The engine's only hard dependency on Last.fm for *the
user* is seed construction; candidates/scoring are user-agnostic.

**Acceptance criteria:**
- A Spotify account with zero Last.fm history gets a full results page (index,
  matrix, ledger) — end-to-end QA with a real test account.
- Seed parity test: for a user with both accounts, Spotify-derived and
  Last.fm-derived seed sets overlap ≥60% on top-20 artists (sanity fixture,
  not a hard product gate — documents the mapping quality).
- Spotify tokens never appear in backend logs or storage (grep test on debug
  logs during an e2e run).
- Time-range picker maps to the existing period pills; blend mode works by
  weighting the three Spotify ranges (unit test on weights).
- Without `NEXT_PUBLIC_SPOTIFY_CLIENT_ID`, the button is hidden and Last.fm
  flow is unchanged.

### F6. One-click playlist export for artist recs

**What:** "Export as playlist" on the results page: top 2 tracks per
recommended artist (via existing `artist.gettoptracks`) → private Spotify
playlist named "Obscurity finds — {date}", using the already-built
`createPlaylist`. Each export is an F2 event.

**Why:** recommendations you can *press play on* get adopted; links you have to
chase don't. Also makes every adopted rec attributable (playlist plays →
scrobbles → F3 closes the loop). Currently the entire playlist code path is
dead.

**Acceptance criteria:**
- E2E: export from a real results page creates a playlist with ≥80% of shown
  artists represented (some artists lack Spotify presence — that's expected;
  the export reports "18 of 25 added").
- Export without a connected Spotify session triggers OAuth then resumes and
  completes (session-storage handoff already exists — verify it survives).
- Playlist is private by default; an `events` row of type `playlist_export` is
  written per export (test).
- Failure of any single track lookup never aborts the export (unit test with a
  stubbed 404).

### F7. Public taste pages + shared-result upgrade (SEO / acquisition)

**What:** Upgrade tonight's persistent share links (`/r/[id]`) into permanent
public taste profiles (`/u/[username]`, opt-in): obscurity index, taste
signature, top discoveries, with proper OG tags and server rendering so links
unfurl on WhatsApp/Twitter/Discord. Add programmatic artist pages
(`/artist/[name]`) for every artist that has ever appeared in
recommendations: listeners, genre, "listeners with taste like X adopt this
artist", listen links.

**Why:** the only acquisition channel today is a PNG. Public pages make every
share a landing page and every obscure artist an SEO surface — thousands of
long-tail pages for artists nobody else writes about. Directly serves the
mission (artist visibility) before CEGE lands.

**Acceptance criteria:**
- `/u/[username]` 404s unless the user opted in (default off); opt-out deletes
  the page (test both).
- Link unfurl: OG title/description/image present in server-rendered HTML
  (curl test, no JS execution).
- Artist pages render from `artist_observations` + `recommendations` data with
  no live Last.fm call on the request path (latency test: p50 < 300ms).
- Lighthouse SEO score ≥ 90 on both page types; sitemap.xml lists them.

## Phase 3 — Break the reach ceiling (data ceiling)

### F8. Pluggable candidate provider + CEGE integration

**What:** Extract a `CandidateSource` seam where `candidates.rs` calls
`getsimilar` (and its mirror in `eval/pipeline.py`), then implement a CEGE
provider: seed artists → CEGE graph neighbors (pgvector similarity + graph
edges from the MusicBrainz/Wikidata ETL). Ship behind a flag; blend mode
(union of Last.fm + CEGE candidates) is the expected winner. The eval harness
A/Bs it exactly like every prior lever — this is why the scoring brain was
kept Python-portable.

**Why:** the single documented unlock. Every closed experiment in
[roadmap.md](roadmap.md) concluded reach is capped by the candidate source,
not the ranking. This is the only feature on this list that can move
pure-discovery hits off the ~78 ceiling.

**Acceptance criteria (the bar is pre-registered, per eval discipline):**
- Eval harness runs the CEGE provider on the de-biased cohort (n=348 protocol:
  126 users × 3 anchors, paired bootstrap) with zero changes to metrics code.
- **Ship gate:** reach CI excludes the 0.05 baseline upward, AND
  pure-discovery hits exceed 78 with a paired-bootstrap CI excluding zero, AND
  obscW@k does not regress. If CEGE candidates don't clear this, the feature
  does not ship (same rule that killed two-hop).
- Serving latency with blend ≤ 1.5× Last.fm-only p50 (candidate fetch is
  parallel; CEGE is a local DB).
- Provider behind `CANDIDATE_SOURCE=lastfm|cege|blend` env; `lastfm` remains
  the default until the ship gate passes.

### F9. Tracks mode, un-gated

**What:** Bring the track pipeline to artist-pipeline standards — fail-closed
error handling (it currently silently drops failed stages and can serve
non-deterministic partials), appetite/underexplored support, populated
`source_seeds` provenance, cross-validation — then remove `TracksComingSoon`.
Sequenced after F8 because the 2026-06-21 deferral reason (same capped graph)
dissolves once candidates come from CEGE; the playlist-export flow (F6) makes
tracks the natural unit.

**Acceptance criteria:**
- Fail-closed parity: injected transient failure in any stage fails the whole
  request rather than returning partials (test per stage — this currently
  fails by design of the old code).
- `source_seeds` non-empty for every returned track (test).
- Appetite slider affects tracks mode identically to artists (e2e).
- Eval: a `track_eval` smoke run on ≥20 users completes and reports the same
  metric family; obscW@k for tracks published as the new baseline (no ship
  gate vs artists — it's a new surface — but the number must exist).

## Phase 4 — The artist side (the mission's other half)

### F10. Artist dashboard ("path to 1000 true fans")

**What:** An artist (or manager) claims their page from F7 (verification:
edit the artist's Last.fm wiki or link from their official site/Bandcamp —
pragmatic v1). Dashboard: listener growth curve (F4 data), distance-to-1000
true-fans estimate (the `true_fans` model from `eval/config.py` finally has a
purpose — as an *artist-facing* metric it was never wrong, it was wrong as a
listener-side ranking gate), where they surface in recommendations (how many
times shown/clicked/saved/adopted, from F1-F3 data), and which genres/taste
clusters adopt them.

**Why:** this is the mission. Every prior phase generates the data that makes
this dashboard non-trivial: observations (F4) give the growth curve,
events (F2) give shown→adopted funnels, CEGE (F8) gives graph context. It's
also the seed of a B2B/artist-services revenue path that the listener app
alone never has.

**Acceptance criteria:**
- An unclaimed artist page shows a "claim this page" flow; claim requires
  passing one of the two verification checks (both e2e-tested with a test
  artist).
- Dashboard renders shown/clicked/saved/adopted counts that exactly match a
  SQL ground-truth query on the events tables (reconciliation test).
- True-fans estimate is computed with the existing `eval/config.py` per-user
  model parameters, unit-tested against 3 hand-computed fixtures.
- No listener-side ranking change of any kind ships with this feature (the
  eval already ruled; diff-scope test asserts `scoring.rs` untouched).

---

## Sequencing and dependencies

```
Tonight (in flight): share persistence, Redis cache, test harness, polish
Phase 1: F1 → F2 → F3 (strict chain), F4 (parallel after F1)
Phase 2: F5, F6, F7 (independent of each other; all need F1; F6 needs F5's OAuth)
Phase 3: F8 (needs CEGE ETL from the CEGE project), F9 (needs F8)
Phase 4: F10 (needs F2/F3/F4 accumulated data — start ~60 days after Phase 1 ships)
```

Phase 1 is deliberately first even though Phase 2 is flashier: every week
without feedback capture is a week of adoption data lost forever, and F4's
time-series only becomes valuable with age.

## Explicitly not on this roadmap

- Any listener-side scoring/ranking lever (two-hop, match-weight, velocity
  ranking tilt, temporal weighting, adaptive backfill, genre-relative or
  true-fans *ceilings*) — all closed by the n=348 re-validation. F8 changes
  the *candidate pool*, which is the one thing the eval said is the bottleneck.
- Social features (leaderboards, friend compare) — fun, but they don't attack
  a ceiling; revisit after Phase 2 ships and retention is measurable.
  *(Challenged in review — friend-compare is the proven growth loop in this
  category (Obscurify/Receiptify); surfaced as taste decision at the gate.)*
- Native apps — the mobile web layout already works; no evidence of need.

---

# /autoplan review — Phase 1 (CEO) — 2026-07-02

Mode: SELECTIVE EXPANSION (autoplan). Voices: Claude primary + independent
Claude subagent (`[subagent-only]` — Codex CLI not installed). Base branch:
main. Design doc: `Arnuv--main-design-20260606-161653.md` (V2 dual-graph,
premises 1–4 accepted there and still sound for the *scoring* layer).

## Step 0A — Premise challenge (GATE: provisionally answered, re-confirm at final gate)

| Premise | Verdict |
|---|---|
| Three ceilings (data/signal/audience) | ACCEPTED — grounded in n=348 eval |
| "CEGE is the only reach unlock" | **CHALLENGED** — zero coverage evidence; ListenBrainz / artist self-submission / Bandcamp adjacency never analyzed. Revised to "CEGE is the leading candidate, pre-gated by a coverage spike and an alternate-source A/B" |
| "Scrobbles after shown = attributed adoption" (F3) | **CHALLENGED** — timestamp-only is the same post-hoc confound the plan criticizes. Revised: condition on interaction events AND add a shown-vs-holdout control arm |
| Unstated: enough traffic to spin the Phase-1 flywheel | **UNKNOWN** — user AFK at gate; assumed low-traffic until confirmed → analytics baseline + traffic gates added |
| Unstated: mission arithmetic closes (MAU × discoveries/user → 1000 fans/artist) | **CHALLENGED (subagent F-1)** — at ~0.22 discoveries/user/run the listener app alone cannot deliver 1000 fans/artist; reframe: listener app = data + acquisition asset; artist side needs its own loop |

## Step 0B — What already exists (leverage map)

| Sub-problem | Existing code |
|---|---|
| File persistence pattern | `KEY_STORE_PATH` (backend/src/main.rs:376) |
| POST endpoint + validation pattern | `POST /api/keys` (main.rs:327) |
| Adoption definition | eval/pipeline.py:549-559 (`min_future_plays=3`) — port, don't reinvent |
| Listener counts already fetched | scoring.rs:148 (≤600 getinfo/run), audit_cache (lastfm.rs:200) — F4 persists what's discarded |
| Spotify PKCE OAuth + playlist create | frontend/src/lib/spotify.ts (dead code today) |
| Share persistence | T1 just landed: shareStore.ts, /api/share, /r/[id] |
| Candidate seam | candidates.rs:25 `CandidateMap` + eval/pipeline.py mirror |
| True-fans model | eval/config.py (`tf_*` params) — reused artist-facing in F10 |
| Shared-cache lock for cron | T2 Redis store (backend/src/cache.rs, landed tonight) |

## Step 0C — Dream state

```
CURRENT STATE                    THIS PLAN                       12-MONTH IDEAL
stateless one-shot toy;   --->   persistent, instrumented, --->  two-sided: attributed obscure
reach 0.05, no feedback,         multi-source, candidate-        discovery for listeners; reach
no artist surface                pluggable, artist dashboard     analytics + self-submission for
                                 seeded                          artists; first-party adoption
                                                                 dataset; reach ceiling broken
```
Delta to ideal: marketplace dynamics (artist self-submission) absent from the
plan as written — surfaced as taste decision.

## Step 0C-bis — Implementation alternatives

- **A. Roadmap as written** (ideal architecture): Effort XL-human/M-CC, Risk Med-High (CEGE dependency + untested premises). Completeness 7/10.
- **B. Minimal viable** (Phase-1-lite + Phase 2 only): Effort M, Risk Low. Never attacks the data ceiling. Completeness 4/10.
- **C. Evidence-first resequencing** ✅ **AUTO-SELECTED (P1 completeness — dominates A, not a coin flip)**: A plus a "Phase 0" spike block (analytics baseline, CEGE coverage check, ListenBrainz A/B, Spotify quota check), control-arm attribution in F3, traffic gates on Phase-1 depth, outcome KPIs + kill criteria per phase, CEGE timebox. Completeness 9/10.

## Step 0D — Cherry-pick ceremony (auto-decided per autoplan principles)

| # | Expansion | Decision | Principle |
|---|---|---|---|
| E1 | Analytics baseline (Vercel Analytics + backend request counter) — S | **ACCEPTED** | P1; traffic unknown is itself a finding |
| E2 | Shown-vs-holdout control arm in F3 — S/M | **ACCEPTED** | P1; kills the attribution confound cleanly |
| E3 | CEGE coverage spike + ListenBrainz A/B as Phase-3 pre-gates — M (eval harness, days) | **ACCEPTED** | P2; blast radius of F8, de-risks the only value-unlock |
| E6 | Spotify quota-extension feasibility check before F5 — S | **ACCEPTED** | P3; one day vs a dead Phase 2 |
| E7 | Outcome KPI + kill criterion per phase — S | **ACCEPTED** | P1; plan currently cannot fail, so cannot be steered |
| E8 | F3 privacy opt-in at first run (not just export/delete) — S | **ACCEPTED** | P1; server-side history polling is a new privacy posture |
| E9 | F7 SEO gate: 50-page indexation pilot before scale — S | **ACCEPTED** | P3; post-Helpful-Content scaled-content risk |
| E11 | CEGE timebox: if ETL not eval-ready by gate date, alternates become Phase 3 — S | **ACCEPTED** | P6; unblocks the only metric-mover |
| E12 | Observability minimums: events/day, adoption rate, cron duration/failure metrics + weekly cron summary log — S | **ACCEPTED** | P1 (Section 8 gap) |
| E13 | Loading/empty/error state ACs for every new surface (discoveries page, /u/, /artist/, dashboard) — S | **ACCEPTED** | P1 (Section 11 gap) |
| E4 | Artist self-submission v0 (form + review queue + candidate-pool injection) — M | **TASTE → final gate** | restructures phase order; strongest mission response |
| E5 | Friend-compare on share page — S/M | **TASTE → final gate** | proven category growth loop vs plan's one-line dismissal |
| E10 | Bandcamp as candidate source + paid-fan surface analysis — M | **DEFERRED → TODOS** | needs research; partially covered by E3 alternates |

## Step 0E — Temporal interrogation (decisions resolved NOW)

- **Hour 1:** users keyed by synthetic id + linked source accounts (NOT lastfm_username as PK) — F5 adds Spotify-only users; Last.fm renames exist.
- **Hour 2-3:** `rec_id` is per-(run, artist) — events attach to the impression, dedupe at analysis time.
- **Hour 4-5:** F3 cron needs a distributed lock (Redis, landed tonight) + resume cursor for partial failures.
- **Hour 6+:** consent copy at first run (E8); export format = JSON of all rows keyed to user.

## Sections 1–11 (evaluated; findings auto-decided, logged in audit trail)

**S1 Architecture** — 3 findings, all folded into ACs: cron single-point/dup-run (Redis lock + resume cursor), events endpoint abuse (rate limit + rec_id validation — already in F2 AC), CandidateSource seam must land in Rust *and* eval Python simultaneously (F8 AC updated).

```
Vercel (Next.js)                        Railway (Rust/axum)              stores
┌──────────────────────┐   /api/discovery  ┌──────────────────┐   ┌──────────────┐
│ page / r/[id] / u/[u]│ ────────────────▶ │ pipeline (seeds→ │──▶│ Last.fm API  │
│ /artist/[name]       │                   │ candidates→score)│   │ (key pool)   │
│ saved / discoveries  │   /api/events     │ events API   NEW │   ├──────────────┤
│ share store (KV)     │ ────────────────▶ │ adoption cron NEW│──▶│ Postgres NEW │
│ Spotify OAuth (PKCE) │                   │ obs writer   NEW │   │ Redis (T2)   │
└──────────────────────┘                   │ CandidateSource  │   │ KV (T1)      │
                                           │ seam:lastfm|cege │   └──────────────┘
                                           └──────────────────┘
```

**S2 Error & Rescue** — registry below; 2 GAPS found → both fixed via ACs (cron resume cursor; F4 UPSERT dedup).

| Codepath | Failure | Rescued? | User sees |
|---|---|---|---|
| discovery + DB down | Postgres unreachable | Y — serve stateless (graceful-fallback rule), log warn | normal results, no persistence |
| POST /api/events | network/5xx | Y — fire-and-forget, client silent, server logs | nothing (by design) |
| adoption cron | Last.fm 429/Error-29 | Y — key-pool bench + backoff (existing) | n/a |
| adoption cron | crash mid-cohort | **was GAP** → resume cursor AC added | n/a |
| F4 observation write | dup (multi-instance) | **was GAP** → `ON CONFLICT DO NOTHING` AC added | n/a |
| Spotify token refresh | expired/revoked | Y — re-auth prompt, keep Last.fm path | "reconnect Spotify" |
| KV share fetch | missing/expired | Y — friendly 404 (T1, verified tonight) | "link expired" page |

**S3 Security** — 4 findings: events forgery (rate limit + validation — covered), /u/ enumeration (uniform 404, opt-in default off — covered), **artist claim verification weak** (wiki-edit is spoofable → F10 AC upgraded: official-site link-back OR Bandcamp/label domain match required; wiki alone insufficient), listening history = PII (consent E8 + export/delete AC). No new injection vectors (parameterized sqlx; no raw SQL).

**S4 Data/UX edge cases** — mapped: double-click save (idempotent PUT), Last.fm username rename (synthetic user id, 0E), zero-scrobble user (existing EmptyState), cron overlap (lock), observation table growth (1 row/artist/day, trivial), stale share payloads after schema change (versioned payload — T1 already validates shape).

**S5 Code quality** — 2 findings: duplicate `MAX_LISTENER_CEILING` consts (scoring.rs:29, track_scoring.rs:24) → unify-config task added P3; vestigial `recommended_by`/`conviction_score` model fields → cleanup task P3.

**S6 Tests** — per-feature ACs already strong (row-count assertions, mutation-tested harness from T4, pre-registered F8 gate). Gaps → added: control-arm eval test, DB-absent fallback integration test (F1 AC had it — kept), cron fixture test (F3 AC had it — kept). Full test-plan artifact lands in Phase 3 (eng).

**S7 Performance** — 3 notes: F4 batch-insert per run (not 600 singles), /artist/[name] serves from DB only (AC exists, p50<300ms), cron paced by key pool (existing infra). No N+1 risks identified in schema as sketched.

**S8 Observability** — was the weakest area → E12 ACCEPTED (metrics: events/day, save/dismiss rate, adoption rate, cron duration+failures, candidate-source reach split; weekly cron summary log; runbook notes for DB-down and cron-stuck).

**S9 Deployment** — migrations additive + auto-run (F1 AC), all features env-gated with graceful fallback (standing rule), Railway still manual-deploy (existing roadmap item, unchanged), rollback = unset env + revert. No zero-downtime concerns at current scale.

**S10 Long-term** — reversibility 4/5 (env-gated, additive DB). Debt accepted knowingly: cron-in-web-process (revisit at scale; lock makes it safe). Path-dependency guarded by synthetic user id. Docs culture strong.

**S11 Design/UX** — state coverage for new surfaces was unspecified → E13 ACCEPTED. Emotional arc note: "Your discoveries" is the first screen that shows the product *working over time* — it should lead with adopted artists, not the index. /plan-design-review runs as Phase 2.

## CEO dual voices — consensus table

```
  Dimension                              Claude(primary)  Subagent   Consensus
  1. Premises valid?                     revise           revise     CONFIRMED (revise)
  2. Right problem to solve?             yes w/ resequence partially CONFIRMED (add arithmetic + resequence)
  3. Scope calibration correct?          gate by traffic  Phase1 too deep  CONFIRMED (traffic gates)
  4. Alternatives sufficiently explored? no (CEGE alts)   no (F-2/F-4)     CONFIRMED (spikes added)
  5. Competitive/market risks covered?   no (doc silent)  no (F-7)         CONFIRMED (competitive page added)
  6. 6-month trajectory sound?           risk: chain      risk: F-10       CONFIRMED (CEGE timebox)
```
6/6 confirmed-with-revisions; no cross-voice disagreement. Codex: N/A (not
installed). Subagent findings F-1..F-10 absorbed: F-1→mission arithmetic
section todo, F-2→E3, F-3→E1+traffic gates, F-4→E4 taste, F-5→E2, F-6→E6+
non-scrobbler attribution spec, F-7→competitive page todo, F-8→E5 taste+E9,
F-9→E7, F-10→E11.

## NOT in scope (unchanged + new)

All prior exclusions stand (scoring levers, tracks-before-CEGE, etc.).
Newly deferred: Bandcamp deep analysis (E10 → TODOS), friend-compare and
artist self-submission pending final-gate decision.

## Dream state delta

With E1-E13 accepted, the plan reaches ~80% of the 12-month ideal's
foundations; the remaining 20% (marketplace dynamics, paid-fan surface) hangs
on the two taste decisions at the gate.

## Failure Modes Registry (CEO pass)

| Codepath | Failure mode | Rescued? | Test? | User sees? | Logged? |
|---|---|---|---|---|---|
| discovery w/ DB down | Postgres unreachable | Y (stateless fallback) | Y (F1 AC) | normal results | Y |
| POST /api/events | 5xx / forged rec_id | Y (best-effort / 400) | Y (F2 AC) | nothing / 400 | Y |
| adoption cron | 429 storm | Y (bench+backoff) | Y (F3 AC) | n/a | Y (E12) |
| adoption cron | crash mid-cohort | Y (resume cursor — added) | Y (added AC) | n/a | Y (E12) |
| F4 obs write | multi-instance dup | Y (UPSERT — added) | Y (added AC) | n/a | Y |
| Spotify refresh | token revoked | Y (re-auth prompt) | Y (F5 AC) | "reconnect" | Y |
| KV share fetch | missing/expired | Y (friendly 404) | Y (T1, verified) | "link expired" | Y |

No RESCUED=N rows remain → **0 CRITICAL GAPS** after added ACs.

## CEO Completion Summary

```
+====================================================================+
| Mode: SELECTIVE EXPANSION (autoplan) | Audit: overnight T1-T5 landed|
| S1 Arch: 3 issues (all → ACs)   | S2 Errors: 7 paths, 2 GAPS fixed |
| S3 Security: 4 (1 High: artist claim verification — AC upgraded)   |
| S4 Data/UX: 6 edge cases mapped, 0 unhandled after ACs             |
| S5 Quality: 2 (P3 tasks)        | S6 Tests: gaps → 3 ACs added     |
| S7 Perf: 3 notes, 0 blockers    | S8 Observability: 1 gap → E12    |
| S9 Deploy: 0 new risks          | S10 Future: reversibility 4/5    |
| S11 Design: 1 gap → E13; /plan-design-review runs as Phase 2       |
| NOT in scope: written | What exists: written | Dream delta: written |
| Error/rescue registry: 7 methods, 0 CRITICAL GAPS after fixes      |
| TODOS proposed: 4 | Scope: 13 proposed, 11 accepted, 2 → gate      |
| CEO plan: written (~/.gstack/.../ceo-plans/2026-07-02-roadmap-10x) |
| Outside voice: claude subagent (codex not installed)               |
| Lake Score: 11/11 auto-decisions chose the complete option         |
| Unresolved: premise gate re-confirm + E4 + E5 (final gate)         |
+====================================================================+
```

# /autoplan review — Phase 2 (Design) — 2026-07-02

Voices: Claude primary + independent design subagent (`[subagent-only]`).
Mockups: skipped — gstack designer needs an OpenAI key (`$D setup`); text-only
review per documented fallback. DESIGN.md exists → all decisions calibrated
against it. Classifier: **APP UI** (personal surfaces) + **HYBRID** (/u/,
/artist/ pages: marketing shell, app content).

## Design pass ratings (before → after plan fixes below)

| Pass | Before | After | What closed the gap |
|---|---|---|---|
| 1. Information architecture | 3/10 | 9/10 | Surface-specs appendix: content order per surface + nav model |
| 2. Interaction states | 2/10 | 8/10 | E13 extended: 5-state row per surface, house patterns (scan bar, `[ERR]` mono) |
| 3. Journey/emotional arc | 5/10 | 9/10 | Pending-first discoveries, dismiss-undo+backfill, dashboard leads with motion |
| 4. AI slop risk | 6/10 | 9/10 | "Inherit ledger/terminal language, no card grids" constraint + chart spec |
| 5. Design system alignment | 7/10 | 9/10 | Rising badge = chrome-free mono (gold stays rationed); tokens reused |
| 6. Responsive & a11y | 3/10 | 8/10 | 44px targets, keyboard nav for card actions, aria-label + table fallback on charts, mobile stacking |
| 7. Unresolved decisions | — | 10 resolved, 2 deferred | identity model final shape → Phase 3 (eng); mockup approval → user |

**Overall: 4/10 → 8.5/10.**

## Surface specs (adopted into the plan — implementer reads this)

- **Identity primitive (BLOCKING before F2, cross-referenced to eng phase):**
  writes (save/dismiss/events) and personal reads (Saved, discoveries,
  `/api/me/*`) require a browser session token minted at first run and bound
  to the username; typed-username alone grants read-only public surfaces.
  Spotify OAuth (F5) upgrades the same identity. Anyone-can-act-as-you is
  explicitly rejected.
- **Navigation model:** no global nav bar. Results top bar gains one quiet
  mono text item ("saved / discoveries", 10px, `--dim`, right of period
  pills, shown only when the user has ≥1 save or ≥2 runs). Landing stays
  blank except a returning-user line under the input ("welcome back — 7
  saved artists →").
- **Your discoveries:** pending state IS the primary state ("12
  recommendations tracked · adoption checked weekly · next check in 3
  days"); hero = adopted artist names (Playfair, results-card weight), then
  prose summary, index sparkline last at footnote scale. One-run users get a
  dot, not a two-point line. Translate mean-listeners into meaning or cut it.
- **Save/dismiss on ArtistCard:** mono 10px text actions in the expanded
  state (hover-reveal on desktop); dismiss collapses the card (existing
  height animation) with 5s inline undo row and backfills rec #26 — dismissal
  is rewarded. Saved marker uses `--text` + filled glyph; gold stays rationed.
- **/u/[username]:** ShareCard hierarchy (index → prose verdict → deepest
  finds → taste signature) + calibration prose on the score + the landing
  input verbatim below ("find your own depth") — visitor conversion is the
  page's KPI. Opted-out/unknown → one designed uniform 404.
- **/artist/[name]:** name+genre → one-line prose positioning → listen links
  above the fold → growth/listeners → quiet "are you {artist}? claim this
  page" footer. **Never render social-proof counts under n=5** — qualitative
  copy instead.
- **Artist dashboard (F10):** lead with motion ("listeners up 12% in 60
  days"), then taste-cluster insight, then reach-framed funnel,
  distance-to-1000 LAST as a path with a next step. Low-data state ("early
  days — first growth reading in N days") is the launch-day state for 100% of
  artists — designed first. Claim flow needs step-by-step instructional UI +
  pending/failed verification states.
- **Spotify entry:** lowercase mono text link under the input ("or continue
  with spotify", 11px, `--muted`, no button chrome); OAuth return lands in
  the existing LoadingState ("reading your library..."); top bar shows
  Spotify display name where the Last.fm username would be.
- **Playlist export:** lives at the head of the artist list (it operates on
  the list), mono text, inline connect note when unconnected; in-progress
  uses the scan-bar pattern; success links to the created playlist.
- **Rising badge:** mono 9px "RISING", `--muted` border, `--text` color —
  no new palette color. Stacking: DUAL SIGNAL first, 6px gap, wraps below
  the name on mobile.
- **Chart spec (add to DESIGN.md):** 1px `--muted` line, `--accent` terminal
  dot, no gridlines, no axis labels except first/last date in mono 9px
  `--dim`, minimum 3 points before drawing a line.

## Litmus scorecard (Claude primary vs design subagent)

```
  Check                                Claude   Subagent  Consensus
  1. Brand unmistakable first screen?  YES      NOT SPEC'D → fixed by surface specs
  2. One strong visual anchor?         YES      NOT SPEC'D → fixed (hero lists)
  3. Scannable by headlines only?      YES      YES        CONFIRMED
  4. Each section has one job?         DISAGREE (/artist/ dual audience) → fixed by B4 hierarchy
  5. Cards actually necessary?         YES      YES        CONFIRMED (cardless system)
  6. Motion improves hierarchy?        YES      YES        CONFIRMED (house animations reused)
  7. Premium without shadows?          YES      YES        CONFIRMED
  Hard rejections triggered:           0        0          CONFIRMED (card-grid risk pre-empted)
```

Design NOT in scope: mockup approval (needs OpenAI key + user at keyboard);
final saved-marker glyph choice (cosmetic, implementer's call within spec).

# /autoplan review — Phase 3 (Eng) — 2026-07-02

Voices: Claude primary + independent eng subagent (`[subagent-only]`, read the
actual Rust/Python/TS code). All findings below are ADOPTED into the plan as
acceptance criteria unless marked otherwise.

## Blocking pre-requisites added to Phase 1 (from converged findings)

1. **Identity layer (all three voices converged — the #1 cross-phase theme).**
   Last.fm web-auth (`auth.getSession`) or Spotify OAuth mints a session bound
   to a **synthetic user id** (usernames normalized case-insensitively; Last.fm
   renames tolerated). Persistence, cron enrollment, save/dismiss, and
   `/api/me/*` require the session; typed-username-only use stays exactly
   today's ephemeral read-only flow. **The backend never polls a user who
   hasn't authenticated and opted in** — no third-party surveillance.
2. **Spotify seed endpoint hardening (eng S1, CRITICAL).** Reshaped per DX-7 to
   `POST /api/discovery` with `{source, seeds, period, appetite}`. The backend
   verifies the short-lived Spotify access token server-side (`/v1/me` +
   top-artists fetch, never persisted) so seed payloads are non-forgeable;
   hard caps (≤50 seeds, ≤2KB names, weight bounds); canonical cache key =
   hash of sorted normalized seeds; per-IP limits (1 concurrent, N/hour,
   trusted-hop X-Forwarded-For only). Does not ship with best-effort limits.
3. **Cron/key-pool isolation (eng T1, CRITICAL scenario).** The adoption cron
   uses dedicated key(s) never shared with the serving pool, ~1 req/s pacing,
   concurrency 1, jittered start, circuit breaker on bench-rate. Gate test:
   mock-Last.fm contention test (cron + live traffic concurrently, live
   success-rate asserted) + crash-mid-cohort resume test.

## Adopted engineering ACs (by finding)

- **Impressions on cache hits (A3/DX-2):** cache-served responses record a
  lightweight impression row referencing the original run, written on the
  response path; `run_id` + per-item `rec_id` become nullable
  `DiscoveryResponse` fields; frontend suppresses events when absent.
- **Events schema (A2/S2):** `events(id, run_id NULL, rec_id NULL, type,
  occurred_at, dedup_key)` + CHECK(one FK set); **UUIDv4 rec ids**
  (capability-style), TTL acceptance window, per-(rec_id,type) uniqueness;
  strict CORS before the write API ships; `sendBeacon`/keepalive for
  click events (else navigation cancels the fetch and undercounts the
  primary metric).
- **DB slow ≠ DB down (E1/H4):** sqlx `acquire_timeout` ≈1s; all persistence
  writes off the response path (bounded queue, drop-and-log when full). F1
  latency AC rewritten: cold-path overhead ≤ X ms with writes off-path;
  cache-hit path byte-identical to today.
- **Cron lock (A5):** Postgres advisory lock + per-user resume cursor in
  Postgres (drops the Redis dependency for correctness; Redis stays a cache).
- **F4 observations (E2/H2):** one batched `INSERT .. ON CONFLICT DO NOTHING`
  per run keyed on normalized artist name (+mbid when present); honest badge
  claim = "rising among artists we surface" (sampling is biased to
  recommended artists); velocity robust to observation gaps.
- **Candidate seam contract (A6):** providers return normalized keys +
  per-seed provenance; blend-dedup unit test with colliding names; entity
  resolution miss-rate budgeted inside the F8 coverage spike.
- **Privacy (S3, extends E8):** consent captured BEFORE cron enrollment;
  `DELETE /api/me/data` unenrolls + purges derived rows; Last.fm ToS check on
  retained scrobble-derived rows.
- **Public pages (S5):** uniform 404 (timing included) for no-user vs
  opted-out; **k-anonymity floor k≈10** on all cohort claims (tightens the
  design phase's n<5 rule for taste-cohort statements); output-encoding on
  artist names in SSR/OG (XSS via artist name is real).
- **Migrations (T4/H3):** additive-only rule per release; CI boots previous
  binary against migrated schema; rollback = revert binary, leave schema.
- **Parity CI (H5):** standing fixture-user parity test between Rust pipeline
  and eval/pipeline.py before the CandidateSource seam multiplies.
- **Empty/nil (E4):** 0/1-seed Spotify payloads map to
  `empty_discovery_response()`; log2(1)=0 weight guard mirrored from eval
  median-of-empty guards; artist-page slugs with collision handling
  ("AC/DC", emoji, trailing dots).
- **Fallback introspection (A4/DX-6):** `GET /api/status` distinguishing
  per-subsystem `disabled` (unset) / `ok` / `error` (set-but-unreachable);
  set-but-failing logs loudly at boot; `STRICT_DEPS=1` fail-fast for prod;
  `persistence: bool` capability on discovery responses so save buttons
  never silently no-op.

## Test diagram (new things → coverage)

```
NEW UX FLOWS                          TEST                        2AM/HOSTILE/CHAOS
save/dismiss + undo + backfill        e2e (vitest+RTL, T4 harness) double-click idempotency
discoveries page (pending-first)      e2e + fixture cron data      one-run user, zero-adoption week
/u/ + /artist/ SSR                    curl SSR tests (T1 pattern)  opted-out leak test, XSS artist name
Spotify OAuth connect + return        e2e w/ SPOTIFY_MOCK=1        expired token mid-export
playlist export partial               unit + e2e                   single-track 404 never aborts
consent opt-in                        e2e                          delete cascades + unenrolls

NEW DATA FLOWS
events ingest                         integration (route tests)   forgery: enumerable ids, replay, 100rps/IP
impressions on cache hit              integration                 cache-hit row references original run
adoption cron                         fixture unit + resume test  contention test vs live traffic (GATE)
observations batch write              unit (UPSERT dedup)         normalization fragmentation test
spotify seed ingest                   fuzz (10k seeds/10MB/NaN)   key-pool burn loop test
control-arm holdout                   eval fixture test           holdout artist scrobbled 3x → NOT attributed

NEW CODEPATHS: CandidateSource dispatch (unit per provider + blend-dedup);
velocity calc (fixtures, ≥2 obs ≥14d); status endpoint (3-state matrix).
NEW JOBS: adoption cron (above). INTEGRATIONS: Postgres (sqlx offline build
green with no DATABASE_URL), Spotify server-side verify (mocked), CEGE
(fixture graph per DX-11).
```

Full test plan artifact: `~/.gstack/projects/abyss-node-obscurity-engine/Arnuv--main-test-plan-20260702.md`

## Eng dual voices — consensus table

```
  Dimension                       Claude(primary)   Subagent          Consensus
  1. Architecture sound?          with fixes        with fixes A1-A6  CONFIRMED (fixes adopted)
  2. Test coverage sufficient?    gaps              gaps T1-T5        CONFIRMED (added above)
  3. Performance risks addressed? mostly (E1,E2)    mostly (E1,E2)    CONFIRMED (fixes adopted)
  4. Security threats covered?    NO as written     NO (S1-S5)        CONFIRMED (criticals → blocking pre-reqs)
  5. Error paths handled?         yes after Phase1  yes + additions   CONFIRMED
  6. Deployment risk manageable?  yes w/ additive   yes w/ additive   CONFIRMED
```

Eng verdict: plan bones good; gaps clustered under "there is no auth" and
"the key pool is shared and the pipeline fails closed" — both now blocking
pre-reqs with gate tests.

# /autoplan review — Phase 3.5 (DX) — 2026-07-02

Voices: Claude primary + independent DX subagent (`[subagent-only]`). Product
type: consumer web app + future B2B API; personas: (a) returning-cold solo
maintainer, (b) future API consumers, (c) AI agents building from docs/.

## TTHW

- Zero-config discovery path: 3 steps / ~5 min today → **preserved by every
  phase** (the plan's best DX property; explicitly tested).
- Full-stack dev after the plan: was heading to ~7 steps / 20-30 min →
  target **≤10 min** via: `postgres` service in docker-compose + documented
  one-liner, `start.sh` offers local-DB setup, `.env.example` updated every
  phase, committed `.sqlx/` offline cache (build green with no DATABASE_URL).

## Adopted DX ACs (by finding)

- **DX-2/3 (versioning):** `"v":1` added to SharePayload NOW (before F4/F8/F9
  widen it); `schema_version` on events/runs; readers ignore unknown keys,
  renderers version-gate; rolling-deploy 400s eliminated.
- **DX-4 (docs map):** every feature AC gains a Docs bullet — F1→
  howto-run-locally + howto-deploy + new reference-schema.md; F2/F3→
  reference-api + reference-eval-harness + new explanation-privacy.md;
  F4→explanation-scoring (velocity no longer always-null); F5/F6→
  reference-api + tutorial + howto-deploy; F7→reference-api routes/OG;
  F8→reference-eval-harness + howto-deploy (CANDIDATE_SOURCE); F9→
  reference-api tracks section.
- **DX-7 (route convention):** source is orthogonal to output —
  `POST /api/discovery` `{source:"spotify", seeds,...}`; GET stays the
  Last.fm sugar; seed schema + max size documented as a public contract.
- **DX-8 (error envelope):** all new endpoints use `{error, code}` (the
  oldest documented convention); every endpoint AC includes an error table
  distinguishing "fix your payload" from "rec expired, drop it" (400 vs
  404/410), 429 with Retry-After, DB-down behavior stated.
- **DX-9:** dev-only manual cron trigger (`cargo run -- poll-adoptions
  --user X` or env-gated admin POST) + last-run timestamp in /api/status.
- **DX-10:** `SPOTIFY_MOCK=1` canned-fixture mode; Spotify app registration
  (localhost redirect URIs) documented in howto-run-locally.
- **DX-11:** committed CEGE fixture graph (few hundred artists, SQL dump) to
  exercise the seam + blend locally; `lastfm` default flag kept.
- **DX-6:** covered in eng phase (/api/status 3-state), credited here — the
  cheapest high-value insurance in the plan: prevents silent weeks-long loss
  of irreplaceable F4 time-series to a typo'd env var.

## Developer journey (9-stage) — condensed

clone(1min) → env(.env.example, 2min) → run(start.sh, 2min: zero-config OK) →
first feature dev(needs compose postgres one-liner, 5min) → test(npm test /
cargo test / eval harness, documented) → debug(/api/status + manual cron
trigger + [ERR] envelopes) → integrate(reference-api error tables) →
deploy(howto-deploy env matrix) → return-cold(docs map keeps reference-api
truthful; handoff docs culture already strong).

Empathy narrative: *"I clone this in November with no memory of July. start.sh
boots it with one key. When saves don't persist I hit /api/status and see
postgres: disabled — intentional, not broken. When I need the cron I trigger
it manually for one user. reference-api.md still matches reality because every
feature AC forced the doc update."*

## DX scorecard

| Dimension | Before | After |
|---|---|---|
| Getting started (<5min) | 8 | 9 |
| API/CLI naming guessable | 5 | 8 |
| Error messages actionable | 4 | 8 |
| Docs findable & complete | 3 | 9 |
| Upgrade path safe | 5 | 8 |
| Dev environment friction | 5 | 8 |
| Escape hatches | 8 | 9 |
| Observability for devs | 4 | 8 |
| **Overall** | **5.3** | **8.4** |

## DX dual voices — consensus table

```
  Dimension                     Claude(primary)  Subagent        Consensus
  1. Getting started < 5 min?   YES (preserved)  YES (preserved) CONFIRMED
  2. Naming guessable?          NO (DX-7)        NO (DX-7)       CONFIRMED (fixed)
  3. Errors actionable?         NO (DX-8)        NO (DX-8)       CONFIRMED (fixed)
  4. Docs findable/complete?    NO (DX-4)        NO (DX-4)       CONFIRMED (fixed)
  5. Upgrade path safe?         NO (DX-3)        NO (DX-3)       CONFIRMED (fixed)
  6. Dev env friction-free?     PARTIAL (DX-5)   PARTIAL (DX-5)  CONFIRMED (fixed)
```

<!-- AUTONOMOUS DECISION LOG -->
## Decision Audit Trail

| # | Phase | Decision | Classification | Principle | Rationale | Rejected |
|---|-------|----------|----------------|-----------|-----------|----------|
| 1 | CEO | Premises: revised (CEGE pre-gated, control-arm attribution) | GATE (provisional — user AFK, re-confirm at final gate) | — | both voices rated original premises critical-risk | original premises |
| 2 | CEO | Traffic: assume low until user confirms; analytics baseline first | GATE (provisional) | P6 | no usage data exists; conservative gating | assume-high |
| 3 | CEO | CLAUDE.md routing rules | deferred to final gate | — | repo write needs explicit consent | auto-commit |
| 4 | CEO | Approach C (evidence-first resequencing) | Mechanical | P1 | dominates A (same scope + de-risking), not a coin flip | A, B |
| 5 | CEO | E1 analytics baseline | Mechanical | P1 | traffic unknown is itself a finding | skip |
| 6 | CEO | E2 shown-vs-holdout control arm | Mechanical | P1 | kills attribution confound; cheap | timestamp-only |
| 7 | CEO | E3 CEGE coverage spike + ListenBrainz A/B pre-gates | Mechanical | P2 | de-risks the only value-unlock, days of harness work | blind CEGE bet |
| 8 | CEO | E6 Spotify quota feasibility check | Mechanical | P3 | 1 day vs dead Phase 2 | skip |
| 9 | CEO | E7 outcome KPIs + kill criteria per phase | Mechanical | P1 | plan cannot fail → cannot be steered | engineering-only ACs |
| 10 | CEO | E8 privacy opt-in at first run | Mechanical | P1 | server-side history polling = new privacy posture | export/delete only |
| 11 | CEO | E9 SEO 50-page indexation pilot gate | Mechanical | P3 | scaled-content classification risk | unGated programmatic pages |
| 12 | CEO | E11 CEGE timebox with alternate fallback | Mechanical | P6 | unblocks the only metric-mover | open-ended dependency |
| 13 | CEO | E12 observability minimums | Mechanical | P1 | Section 8 gap | ship blind |
| 14 | CEO | E13 state-coverage ACs for new surfaces | Mechanical | P1 | Section 11 gap | unspecified states |
| 15 | CEO | E10 Bandcamp analysis → TODOS | Mechanical | P3 | needs research; E3 covers nearest need | build now |
| 16 | CEO | E4 artist self-submission v0 | **TASTE → gate** | — | both voices favor it; restructures phases; user context needed | — |
| 17 | CEO | E5 friend-compare on share page | **TASTE → gate** | — | proven category loop vs plan's dismissal; K-factor vs SEO bet | — |
| 18 | CEO | S3: artist claim requires official-site/label link-back (wiki alone spoofable) | Mechanical | P1 | weak verification = fake artist dashboards | wiki-edit only |
| 19 | CEO | S2 gaps: cron resume cursor + F4 UPSERT dedup ACs | Mechanical | P1 | silent-failure rule | leave gaps |
| 20 | CEO | S5: unify ceiling consts + drop vestigial fields (P3 tasks) | Mechanical | P4 | DRY | leave dupes |
| 21 | Design | Identity primitive = blocking pre-req before F2 (session bound to synthetic id) | Mechanical (structural) | P1 | all 3 voices converged; privacy exploit as written | typed-username writes |
| 22 | Design | Nav model: quiet top-bar entry + returning-user line; no global nav | Mechanical (structural) | P5 | preserves blank-landing thesis | hamburger/tab bar |
| 23 | Design | Discoveries page: pending state is primary; names are hero, sparkline footnote | Mechanical (structural) | P1 | default state was worst state | stat-led dashboard |
| 24 | Design | /u/ conversion affordance (landing input verbatim + calibration prose) | Mechanical | P1 | acquisition page was a dead end | content-only page |
| 25 | Design | Save/dismiss spec: expanded-state mono actions, undo + backfill | Mechanical | P5 | tap-target collision; dismissal rewarded | unspecified buttons |
| 26 | Design | Spotify entry as mono text link; OAuth return → LoadingState | Mechanical | P5 | DESIGN.md blank-landing rule | branded button |
| 27 | Design | Rising badge chrome-free mono (no new palette color) | Mechanical | P5 | gold rationed by DESIGN.md; word carries meaning | palette extension |
| 28 | Design | Artist page hierarchy + no social-proof counts under n=5 | Mechanical | P1 | small-n reads as product failure | raw counts |
| 29 | Design | Dashboard leads with motion/insight; distance-to-1000 last; low-data state first | Mechanical | P1 | mission surface must not demoralize its beneficiary | deficit-led scoreboard |
| 30 | Design | Chart spec paragraph added to DESIGN.md | Mechanical | P5 | prevents chart-library default slop | implementer improvises |
| 31 | Eng | Spotify seeds: server-side token verify + hard caps + canonical cache key | Mechanical (security) | P1 | unauthenticated shared-key-pool burner | best-effort limits |
| 32 | Eng | Cron key isolation + pacing + circuit breaker + contention gate test | Mechanical (security) | P1 | weekly self-DoS via fail-closed pipeline | shared pool |
| 33 | Eng | Impressions on cache hits; UUIDv4 rec ids; events schema widened + dedup_key; strict CORS; sendBeacon | Mechanical | P1 | attribution dataset integrity | sequential ids, plain fetch |
| 34 | Eng | Postgres advisory lock (not Redis) for cron; writes off response path w/ acquire_timeout | Mechanical | P5 | fewer deps; DB-slow ≠ DB-down | Redis lock, inline awaits |
| 35 | Eng | k-anonymity k≈10 on cohort claims; additive-only migrations + prev-binary CI; Rust↔eval parity CI | Mechanical | P1 | deanonymization; 2am migration incident; dual-impl drift | none |
| 36 | DX | POST /api/discovery {source} route shape; {error,code} envelope; SharePayload v1 + schema_version | Mechanical | P5 | guessable API, versioned payloads | /api/discovery/spotify |
| 37 | DX | Docs bullet per feature AC; compose postgres + sqlx offline cache; SPOTIFY_MOCK; manual cron trigger; CEGE fixture | Mechanical | P1 | docs-as-contract for agents + cold maintainer | undocumented services |
| 38 | DX | /api/status 3-state (disabled/ok/error) + persistence capability flag | Mechanical | P1 | silent-degrade would eat irreplaceable F4 data | silent fallback only |

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 1 | ISSUES_OPEN (via /autoplan) | 13 proposals, 11 accepted, 1 deferred, 2 → gate |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — (Codex CLI not installed) | subagent-only mode all phases |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR (via /autoplan) | 19 issues, 0 critical gaps after fixes — all adopted as ACs |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | ISSUES_OPEN (via /autoplan) | score 4/10 → 8.5/10, 10 decisions |
| DX Review | `/plan-devex-review` | Developer experience gaps | 1 | CLEAR (via /autoplan) | score 5.3/10 → 8.4/10, TTHW 5min preserved |

**CROSS-MODEL:** Codex unavailable — all four phases ran primary + independent
Claude subagent. Cross-PHASE themes (independent voices converging): (1)
**identity layer missing** — flagged independently by design, DX, and eng;
highest-confidence finding of the review; now a blocking Phase-1 pre-req. (2)
**shared key pool as attack/self-DoS surface** (eng seed-endpoint + cron
findings). (3) **attribution integrity** (CEO control-arm + eng cache-hit
impressions + DX rec_id plumbing).

**VERDICT:** ENG + DX CLEARED; CEO + DESIGN issues_open pending the final
approval gate (premises re-confirm, E4, E5, mockup approval). Ready to
implement Phase 0 + Phase 1 pre-reqs once the gate closes.

**UNRESOLVED DECISIONS:**
- Premise gate re-confirmation (answered provisionally; default: final as written by Phase-0 completion)
- E4 artist self-submission v0 (default: TODOS.md P2)
- E5 friend-compare on share page (default: TODOS.md P2)
- Design mockup approval (needs OpenAI key for the gstack designer + user at keyboard)
- CLAUDE.md skill-routing rules (repo write deferred pending consent)
